apiVersion: v1
kind: Secret
metadata:
  name: dockerconfigjson
  namespace: cinemaabyss
data:
  # Replace with base64-encoded ~/.docker/config.json if your GHCR package is private.
  # Current value is base64('{"auths":{}}\n') and is valid for public images.
  .dockerconfigjson: eyJhdXRocyI6e319Cg==
type: kubernetes.io/dockerconfigjson
